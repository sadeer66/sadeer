name: Build FurniPlan Windows Installer

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - 'index.html'
      - 'v85-final-patch.js'
      - 'package.json'
      - 'desktop/**'
      - 'manifest.webmanifest'
      - 'icons/**'
      - 'build/furniplan.ico'
      - '.github/workflows/desktop-build.yml'

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        shell: pwsh
        run: npm install --no-audit --no-fund

      - name: Build Windows installer
        shell: pwsh
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        run: npm run dist:win

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: FurniPlan-V89-Windows-Installer
          path: dist/*.exe
          if-no-files-found: error
          retention-days: 30

# FurniPlan V86 repaired build trigger

# rebuild-v86

# FurniPlan V88 clean splash rebuild

# FurniPlan V89 fixed app rebuild
